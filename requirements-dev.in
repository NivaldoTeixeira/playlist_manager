# Dependências só de desenvolvimento. O Render instala apenas requirements.txt.
#
# Gerado com:  pip-compile requirements-dev.in
# Uso:         pip install -r requirements.txt -r requirements-dev.txt && pytest
pytest==9.0.1
pytest-asyncio==1.3.0
ruff==0.15.4
