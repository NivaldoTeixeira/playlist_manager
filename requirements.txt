# Conjunto completo e fixado, testado no Python 3.11.
# As transitivas também estão pinadas de propósito: pinar só as diretas ainda
# deixava httpx, starlette e afins flutuarem, que é justamente por onde um
# redeploy quebra sozinho meses depois.
#
# Para atualizar: mexa nas diretas abaixo, rode
#   pip install -r requirements.txt --upgrade && pip freeze
# e regrave este arquivo com o resultado, depois de testar.

# --- diretas ---
fastapi==0.141.1
uvicorn==0.52.1
python-telegram-bot==21.4
spotipy==2.26.0
requests==2.34.2
openai==2.53.0
python-dotenv==1.2.2

# --- transitivas ---
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.14.2
certifi==2026.7.22
charset-normalizer==3.4.9
click==8.4.2
distro==1.9.0
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.18
jiter==0.16.0
pydantic==2.13.4
pydantic_core==2.46.4
redis==8.1.0
sniffio==1.3.1
starlette==1.6.0
tqdm==4.70.0
typing-inspection==0.4.2
typing_extensions==4.16.0
urllib3==2.7.0
