[pytest]
# Os módulos ficam na raiz do projeto, sem pacote; isso os deixa importáveis.
pythonpath = .
testpaths = tests
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning:starlette.*
